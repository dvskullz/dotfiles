# d's dotfiles
d's dotfiles
create .fonts JetBrainMono Nerd fonts
install mpd ncmpcpp arttime and create alias 
For battery consumption use tlp tlp-rdw and cpu power(cpupower frequency-set -g powersave)
sof-firmware for audio fix pipewire (no audio output)
all packages realted to usb connectivity for arch linux
